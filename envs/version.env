# include version number from docker container not managed by the HPI-Schul-Cloud team
# and the same over all instances. Other docker tags are manages in the corisponding
# env file (e.g. develop.env)

POSTGRESS_DOCKER_TAG=9.5-alpine
REDIS_DOCKER_TAG=6.0-alpine
MONGODB_DOCKER_TAG=4.2